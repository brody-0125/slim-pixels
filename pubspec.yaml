name: slim_pixels
description: Bounded native image transforms with SIMD resize and validated JPEG encoding.
version: 0.1.2
publish_to: none
environment:
  sdk: '>=3.10.0 <4.0.0'
dependencies:
  ffi: ^2.1.4
