name: slim_pixels
description: Bounded native image transforms with SIMD resize and validated JPEG encoding.
version: 0.1.2
repository: https://github.com/brody-0125/slim-pixels
issue_tracker: https://github.com/brody-0125/slim-pixels/issues
platforms:
  windows:
  linux:
topics:
  - image-processing
  - image-resizing
  - simd
  - ffi
  - rust
environment:
  sdk: '>=3.10.0 <4.0.0'
dependencies:
  ffi: ^2.1.4
  hooks: ^2.2.0
  code_assets: ^1.2.1
  crypto: ^3.0.6

dev_dependencies:
  lints: ^6.0.0
